import { createPercentagePlaces } from '@reduxjs/toolkit'

export const percentagePlacesSlice = createSlice({
  name: 'percentatge',
  initialState: {
    percentatgePlaces: []
  },
  reducers: {
    
  }
})

export default percentagePlacesSlice.reducer